# Core App

Core is the required system App for dygo.

It provides the foundation dygo needs before business apps can run: users, roles, permissions, sessions, installed apps, ledgers, fixtures, and patches.

The first scaffold only defines the app contract. Runtime behavior will be added through focused follow-up tasks.
