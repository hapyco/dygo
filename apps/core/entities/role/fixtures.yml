entity: role
match: [name]
records:
  - name: studio-member
    label: Studio Member
    description: Baseline role for users who can sign in to Studio.
    enabled: true

  - name: system-manager
    label: System Manager
    description: Manages users, roles, permissions, and system settings.
    enabled: true
