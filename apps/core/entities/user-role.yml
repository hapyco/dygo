name: user-role
label: User Role
plural-name: user-roles
plural-label: User Roles
description: Role assignment for a User.
fields:
  - name: user
    label: User
    type: link
    required: true
    index: true
    options:
      entity: user

  - name: role
    label: Role
    type: link
    required: true
    index: true
    options:
      entity: role

constraints:
  - type: unique
    fields: [user, role]
