name: activity
label: Activity
description: Timeline entry for meaningful Record and system events.
naming:
  strategy: random
fields:
  - name: kind
    label: Kind
    type: select
    required: true
    index: true
    default: record
    options:
      values:
        - record
        - comment
        - workflow
        - job
        - email
        - attachment
        - auth
        - system

  - name: operation
    label: Operation
    type: select
    required: true
    index: true
    options:
      values:
        - create
        - update
        - delete
        - restore
        - comment
        - workflow-transition
        - job-completed
        - email-sent
        - attachment-added
        - login
        - logout
        - system

  - name: status
    label: Status
    type: select
    required: true
    index: true
    default: success
    options:
      values:
        - success
        - failed

  - name: entity
    label: Entity
    type: link
    index: true
    options:
      entity: entity

  - name: record-id
    label: Record ID
    type: bigint
    index: true

  - name: actor
    label: Actor
    type: link
    index: true
    options:
      entity: user

  - name: title
    label: Title
    type: text
    required: true

  - name: message
    label: Message
    type: long-text

  - name: changes
    label: Changes
    type: json

  - name: snapshot
    label: Snapshot
    type: json

  - name: details
    label: Details
    type: json

indexes:
  - name: by-record
    fields: [entity, record-id]

  - name: by-kind-operation
    fields: [kind, operation]
